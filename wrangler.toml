# Cloudflare deployment as a static-assets Worker / Pages project.
# `npm run build` produces dist/; deploy that as static assets at the edge.
# No Worker script — this is a pure static site.
name = "superlookup"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
