# Cloudflare deployment as a static-assets site.
# `npm run build` produces dist/, uploaded as static assets at the edge.
# No Worker script — pure static site (same shape as the superterm repo).
name = "superlookup"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
